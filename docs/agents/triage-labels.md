# Triage 标签

本项目使用以下默认标签：

| 角色 | 标签 |
|---|---|
| 需要分流 | `needs-triage` |
| 需要补充信息 | `needs-info` |
| 可由 Agent 处理 | `ready-for-agent` |
| 需要人工处理 | `ready-for-human` |
| 不处理 | `wontfix` |

任务文件中的 `Status:` 或相关 triage 状态应使用上述标签字符串。
